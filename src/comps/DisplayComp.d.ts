import { ReactNode } from "react";
interface IAlertWrapperProps {
    children: ReactNode;
    className?: string;
}
export declare const AlertWrapper: ({ children, className }: IAlertWrapperProps) => import("react/jsx-runtime").JSX.Element;
export {};
